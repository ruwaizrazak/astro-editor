// DEPRECATED: This test was for the monolithic useAppStore which has been decomposed
// into useEditorStore, useProjectStore, and useUIStore.

import { describe, it } from 'vitest'

describe.skip('Autosave Store Operations (DEPRECATED)', () => {
  it('autosave tests moved to editorStore', () => {
    // Autosave functionality is now in useEditorStore
  })
})
